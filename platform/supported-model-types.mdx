---
title: 'Supported Model Types'
description: 'Which types of AI models are supported'
icon: 'head-side-gear'
---

Currently only `Image Generation` models with `text inputs` are supported. More model and input types will be added in the future.